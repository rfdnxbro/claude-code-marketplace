name: セキュリティスキャン

on:
  pull_request:
  workflow_dispatch:

jobs:
  secret-scan:
    name: シークレットスキャン
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: GitLeaksでシークレットをスキャン
        uses: gacts/gitleaks@v1
